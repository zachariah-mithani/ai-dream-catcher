{
  "name": "ai-dream-catcher-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
